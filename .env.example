# Listener (local)
LISTEN_HOST=127.0.0.1
LISTEN_PORT=8443

# Upstream TLS server (TEMP: passthrough to example.org:443, REVISE LATER)
TARGET_HOST=example.org
TARGET_PORT=443

# Logging
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_FILE=metrics.jsonl